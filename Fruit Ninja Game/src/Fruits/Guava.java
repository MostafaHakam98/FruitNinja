package Fruits;

public class Guava extends fruitObject {
	
	public Guava (int initX, int maxHeight, double C, double speedFactor) {
		setArguments(initX, 719, maxHeight, C, speedFactor);
		setFruitType(fruitType.GUAVA);
	}
}
